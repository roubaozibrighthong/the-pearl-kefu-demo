# CLAUDE.md — The Pearl 自进化客服 Agent Demo

> 本文件是 Claude Code 的项目规则文件。每次新对话开始时，Claude 会首先读取本文件以了解项目规则。

## 核心规则

1. **默认使用中文回复。**
2. 允许根据当前任务需要读取项目文件夹 `/Users/hongyiming/Documents/电商客服_副本` 之外的文件。项目外内容默认仅限只读访问；未经用户针对具体操作明确授权，不得在项目外创建、修改、移动、重命名或删除任何文件。
3. **首次实施前必须先进行只读盘点**；向用户说明拟执行操作及影响并获得明确同意后，方可创建或修改项目内文件。
4. **禁止删除任何文件。**
5. 任何需要用户确认的操作，必须先说明拟执行的操作及影响，并等待用户明确同意后再执行。
6. 无法判断如何处理的文件，应加入待确认清单，不得擅自或强行处理。
7. **每次执行完成后，必须更新项目根目录下的 `整理日志.md`**，记录执行时间、已执行操作、未处理事项和待确认清单。

## 项目信息

- **项目名称**：The Pearl — 家电电商自进化客服 Agent（黑客松 Demo）
- **技术栈**：Python + Streamlit + 本地 JSON + GEP/EvoMap
- **Demo 入口**：`app.py`
- **项目简报**：见 `BRIEF.md`

## 每次对话开场

以下两个步骤必须在任何修改操作之前完成：
1. 阅读 `BRIEF.md`，理解项目目标、P0 范围和边界
2. 完成只读盘点，向用户报告当前状态

## 协作须知

- 本项目为可分享的副本版本，只包含 Demo 运行所需的最小文件集
- 原始完整项目位于 `/Users/hongyiming/Documents/电商客服/`
- **禁止修改原始项目中的任何文件**
- 伙伴拿到本项目后，需自行执行 `pip install -r requirements.txt` 和 `npm install` 来创建虚拟环境和依赖
