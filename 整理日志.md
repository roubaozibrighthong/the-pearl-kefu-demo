# 整理日志

## 2026-06-21 01:00 — 项目初始化

### 执行操作
- 创建 `电商客服_副本/` 目录结构
- 从 `电商客服/` 只读复制 Demo 核心文件（共 18 个文件）：
  - 顶层文件：app.py, gep_integration.py, requirements.txt, package.json, package-lock.json, .gitignore, .env.example
  - data/：demo_state.json, knowledge.json, rules.json, golden_cases.json
  - scripts/：gep_bridge.mjs, evolver_bridge.cjs
  - tests/：test_demo_flow.py, test_gep_integration.py, test_gep_bridge.mjs, test_state.json
- 创建 `CLAUDE.md` — 项目规则文件（Claude Code 自动读取）
- 创建 `BRIEF.md` — 项目目标、边界与验收标准
- 初始化 git 仓库，所有文件已 staged

### 未复制内容（非 Demo 必需或可由伙伴自行生成）
- .venv/ (391M) → 伙伴自行 pip install
- node_modules/ (29M) → 伙伴自行 npm install
- 01_原始数据/ ~ 05_整理过程/ → 历史数据
- 所有 .jpg/.png/.pdf/.docx → 非必需媒体文件
- tmp/, outputs/, __pycache__/, assets/, memory/ → 生成物或文档附件
- runtime/gep/ 和 runtime/evolver/ 内容 → 运行时自动生成
- 各种 Markdown 文档（EvoMap 解读、作战室等）→ 非 Demo 运行必需

### 项目大小
- 副本文件大小：约 140KB（不含 .git），312K（含 .git）
- 原文件夹大小：约 600+ MB
- 精简比例：99.95%

### 待确认清单
- 无

### 伙伴上手步骤
1. `python -m venv .venv && source .venv/bin/activate`
2. `pip install -r requirements.txt`
3. `npm install`
4. `cp .env.example .env`
5. `.venv/bin/streamlit run app.py`
