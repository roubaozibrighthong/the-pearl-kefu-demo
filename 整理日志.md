# 整理日志

## 2026-06-21 01:00 — 项目初始化

### 执行操作
- 创建 `电商客服_副本/` 目录结构
- 从 `电商客服/` 只读复制 Demo 核心文件（共 18 个文件）：
  - 顶层文件：app.py, gep_integration.py, requirements.txt, package.json, package-lock.json, .gitignore, .env.example
  - data/：demo_state.json, knowledge.json, rules.json, golden_cases.json
  - scripts/：gep_bridge.mjs, evolver_bridge.cjs
  - tests/：test_demo_flow.py, test_gep_integration.py, test_gep_bridge.mjs, test_state.json
- 创建 `CLAUDE.md` — 项目规则文件（Claude Code 自动读取）
- 创建 `BRIEF.md` — 项目目标、边界与验收标准
- 初始化 git 仓库，所有文件已 staged

### 未复制内容（非 Demo 必需或可由伙伴自行生成）
- .venv/ (391M) → 伙伴自行 pip install
- node_modules/ (29M) → 伙伴自行 npm install
- 01_原始数据/ ~ 05_整理过程/ → 历史数据
- 所有 .jpg/.png/.pdf/.docx → 非必需媒体文件
- tmp/, outputs/, __pycache__/, assets/, memory/ → 生成物或文档附件
- runtime/gep/ 和 runtime/evolver/ 内容 → 运行时自动生成
- 各种 Markdown 文档（EvoMap 解读、作战室等）→ 非 Demo 运行必需

### 项目大小
- 副本文件大小：约 140KB（不含 .git），312K（含 .git）
- 原文件夹大小：约 600+ MB
- 精简比例：99.95%

### 待确认清单
- 无

## 2026-06-21 01:15 — 推送至 GitHub

### 执行操作
- 通过 GitHub API 创建公开仓库 `the-pearl-kefu-demo`
- 配置 remote 并推送 main 分支（20 个文件，2770 行）
- 仓库地址：https://github.com/roubaozibrighthong/the-pearl-kefu-demo

### 待确认清单
- ⚠️ **安全提醒**：本次用于创建仓库的 Personal Access Token 已暴露在 shell 历史中，建议立即删除：https://github.com/settings/tokens

### 伙伴上手步骤
1. `python -m venv .venv && source .venv/bin/activate`
2. `pip install -r requirements.txt`
3. `npm install`
4. `cp .env.example .env`
5. `.venv/bin/streamlit run app.py`

## 2026-06-21 07:32 — 副本可运行性验证

### 执行操作
- 创建 .venv 虚拟环境
- pip install -r requirements.txt（streamlit 1.58.0，全部成功）
- npm install（56 packages，0 vulnerabilities）
- cp .env.example .env
- 运行 Python 单元测试（8 项）
- 启动 Streamlit App 验证 HTTP 可达（localhost:8501 → 200）
- 停止 App 进程

### 验证结果

**测试：8/8 全部通过 ✅**
- test_complete_evolution_flow_and_persistence ✅
- test_high_risk_query_stops_self_service ✅
- test_non_matching_question_does_not_reuse_capsule ✅
- test_state_file_is_valid_json ✅
- test_build_capsule_uses_verified_sdk_asset ✅
- test_evolver_parses_recorded_session ✅
- test_record_agent_turn_writes_sanitized_openclaw_jsonl ✅
- test_sanitize_text_masks_phone_and_long_ids ✅

**App 启动：正常 ✅** — 本地访问 http://localhost:8501

### 待确认清单
- 无。副本已验证可运行，伙伴 clone 后按上述步骤即可启动。
